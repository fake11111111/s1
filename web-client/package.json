{
  "name": "web-client",
  "version": "1.0.0",
  "description": "Web client for the real-time chat and video calling application",
  "main": "simple-server.js",
  "scripts": {
    "start": "node simple-server.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "express": "^4.18.2"
  }
}